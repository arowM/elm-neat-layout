{
  "private": true,
  "scripts": {
    "format": "elm-format src",
    "start": "elm-doc-preview",
    "test": "elm-verify-examples --fail-on-warn --run-tests && elm-analyse && elm-format --validate src && elm make --docs=/tmp/docs.json"
  },
  "devDependencies": {
    "elm-analyse": "^0.16.5",
    "elm-doc-preview": "5.0.5",
    "elm-format": "^0.8.3",
    "elm-test": "0.19.1"
  },
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-verify-examples": "5.0.0"
  }
}
